try {
  $('<%= j(render :template => 'deposits/new', :formats => [:html]) %>').dialog2({
    title: "<%= t('deposit', default: "Deposit") %>",
    id: "new-quote-deposit"
  });
} catch(e) {
  console.log(e);
}