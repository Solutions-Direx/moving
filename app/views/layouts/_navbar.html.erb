<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to(current_account.company_name.present? ? current_account.company_name : "Moving Company", root_path, :class => "brand") %>
      <div class="nav-collapse">
        <ul class="nav pull-right">
          <% if user_signed_in? %>
            <li><%= link_to current_user.full_name, '#' %></li>
            
            <% if current_user.manager? %>
                <li class='dropdown'>
        		  <a class="dropdown-toggle" data-toggle="dropdown" href="#">Account <b class="caret"></b></a>
        		  <ul class="dropdown-menu">
        			<li><%= link_to 'Settings', account_path %></li>
        		    <li><%= link_to 'Users', users_path %></li>
        		    <li class="divider"></li>
        		    <li><%= link_to 'Storages', storages_path %></li>
        		  </ul>
        		</li>
              <% end %>
            
            <li><%= link_to 'Logout', destroy_user_session_path, :method => :delete %></li>
          <% else %>
            <li><%= link_to 'Login', new_user_session_path %></li>
          <% end %>
        </ul>
      </div><!--/.nav-collapse -->
    </div>
  </div>
</div>