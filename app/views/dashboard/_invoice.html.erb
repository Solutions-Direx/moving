<% invoice = Invoice.find(result.searchable_id) %>
<%= status_tag(result.searchable_type, level: "inverse") %> <%= link_to invoice.code, quote_invoice_path(invoice.quote.code) %> || <%= invoice.client.name %> || <%= number_to_currency(invoice.total) %>